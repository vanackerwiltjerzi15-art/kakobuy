# 项目需求文档：Kakobuy SEO 博客网站部署

## 1. 项目概述

在 Cloudflare Pages 平台上，利用已有子域名 `https://kakobuy-7zh.pages.dev`，将本地已生成的 SEO 文章部署为一个**纯静态博客网站**。网站需符合 Google 搜索引擎 SEO 规则，兼顾用户阅读习惯。

### 1.1 网站性质（重要）

- **纯静态页面网站**：所有页面均为预生成的 HTML 文件，无后端服务器、无数据库、无动态渲染
- **无交互功能**：不需要用户登录、评论、表单提交、购物车等任何后端功能
- **内容固定**：文章内容在构建时已生成，用户访问时仅展示静态 HTML 内容
- **技术栈极简**：仅需 HTML + CSS + 少量 JavaScript（仅用于导航等基础功能），不需要 Node.js、PHP、Python 等运行环境

### 1.2 静态页面优势

- **加载极快**：无需服务器处理，Cloudflare CDN 直接分发静态文件
- **安全性高**：无数据库、无后端接口，不存在 SQL 注入、XSS 等安全风险
- **成本为零**：Cloudflare Pages 免费额度足够使用，无需购买服务器
- **维护简单**：更新内容只需修改源文件，无需维护服务器环境

## 2. 目标用户

- 使用英语搜索关键词的海外买家
- 目标平台：Kakobuy（跨境电商采购平台）
- 用户类型：普通消费者、代购/转售商

## 3. 源文件信息

- 本地路径：`D:\kakobuy\`
- 文件格式：Markdown (.md) 文件
- 文件内容：已写好的 SEO 文章，涵盖关键词如 "gutefrage kakobuy spreadsheet"、"quora kakobuy spreadsheet" 等
- 文章数量：约 10-20 篇

## 4. 技术实现要求

### 4.1 平台选择

- **部署平台**：Cloudflare Pages（已创建项目 `kakobuy`，子域名 `https://kakobuy-7zh.pages.dev`）
- **代码托管**：GitHub（公共仓库，方便 Google 爬取和 Cloudflare 自动部署）
- **网站类型**：**纯静态网站** — 所有页面都是预先构建好的 HTML 文件，用户访问时直接获取文件内容，无需服务器端处理
- **静态生成器**：使用 VitePress、Hugo 或 Astro 等轻量级静态网站生成器（推荐 VitePress，配置简单，适合小白）
- **无需后端**：不使用任何服务器端语言（PHP/Node.js/Python 等），不使用数据库（MySQL/MongoDB 等），不处理任何用户输入

### 4.2 网站结构

```
根目录/
── index.html              → 博客首页（静态页面，文章列表）
├── posts/
│   ├── gutefrage-kakobuy-spreadsheet/
│   │   └── index.html       → 静态页面，URL: kakobuy-7zh.pages.dev/posts/gutefrage-kakobuy-spreadsheet/
│   ├── quora-kakobuy-spreadsheet/
│   │   └── index.html       → 静态页面，URL: kakobuy-7zh.pages.dev/posts/quora-kakobuy-spreadsheet/
│   └── [其他文章目录]/
│       ── index.html       → 静态页面
├── sitemap.xml              → 静态 XML 文件，列出所有页面 URL
├── robots.txt               → 静态文本文件，引导爬虫
└── 404.html                 → 静态 404 页面
```

**说明**：以上所有文件均为静态文件，构建完成后直接上传至 Cloudflare Pages 即可。用户访问时，Cloudflare 直接从 CDN 节点分发文件，无需任何服务器端处理。

### 4.3 SEO 技术要求（关键）

每篇文章均为**静态 HTML 文件**，在构建时已生成完整内容。HTML 必须包含：

**Meta 标签：**

```html
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>文章标题 - Kakobuy Spreadsheet Guide 2026</title>
<meta name="description" content="150-160字符的页面描述，包含关键词">
<meta name="keywords" content="关键词1, 关键词2, 关键词3">
<meta name="robots" content="index, follow">
<meta name="googlebot" content="index, follow">
<link rel="canonical" href="完整页面URL">
```

**Open Graph（社交媒体分享）：**

```html
<meta property="og:title" content="文章标题">
<meta property="og:description" content="页面描述">
<meta property="og:type" content="article">
<meta property="og:url" content="完整页面URL">
<meta property="og:image" content="封面图片URL">
```

**结构化数据（JSON-LD）：**

```html
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Article",
  "headline": "文章标题",
  "description": "文章描述",
  "datePublished": "2026-05-27",
  "dateModified": "2026-05-27",
  "author": {
    "@type": "Person",
    "name": "Kakobuy Spreadsheet Guide"
  },
  "publisher": {
    "@type": "Organization",
    "name": "Kakobuy Spreadsheet Guide",
    "logo": {
      "@type": "ImageObject",
      "url": "https://kakobuy-7zh.pages.dev/logo.png"
    }
  }
}
</script>
```

**标题层级：**

- 每页只有一个 `<h1>`（文章主标题）
- 合理使用 `<h2>`, `<h3>` 等子标题
- 关键词自然分布在标题和正文中

**URL 结构：**

- 使用小写 + 连字符（kebab-case）
- 例如：`/posts/gutefrage-kakobuy-spreadsheet/`
- 避免特殊字符和空格

### 4.4 用户体验要求

- **响应式设计**：适配手机、平板、桌面端（通过静态 CSS 实现）
- **加载速度**：首屏加载时间 < 3秒（Cloudflare CDN 直接分发静态文件，无需服务器处理）
- **字体大小**：正文不小于 16px，行高 1.6-1.8
- **配色方案**： 深蓝色背景 + 白色和黄色文字（高对比度，易读）
- **导航结构**：首页展示静态文章列表，分类清晰
- **内部链接**：相关文章之间通过静态超链接互相跳转，提升爬虫抓取和用户留存
- **页首/页脚**：静态导航菜单和版权信息（所有页面共享相同结构）
- **无动态功能**：不需要搜索框、评论系统、用户登录等动态功能

### 4.5 Google 爬虫优化

- **sitemap.xml**：静态 XML 文件，构建时自动生成所有页面 URL 列表
- **robots.txt**：静态文本文件，允许 Google 爬虫抓取所有公开页面
- **404 页面**：静态 404 页面，引导用户返回首页
- **移动端友好**：通过 Google Mobile-Friendly 测试（静态 CSS 响应式布局）
- **Core Web Vitals**：LCP < 2.5s, FID < 100ms, CLS < 0.1（静态页面天然具备优势）
- **爬虫友好性**：静态 HTML 内容无需 JavaScript 渲染即可被 Google 爬虫直接读取，索引效率极高

## 5. 部署流程

### 5.1 本地 Git 初始化

```bash
cd D:\kakobuy
git init
git config --global user.name "用户名"
git config --global user.email "邮箱"
```

### 5.2 GitHub 仓库创建

1. 在 GitHub 创建公共仓库，例如 `kakobuy-blog`
2. 本地关联并推送：

```bash
git remote add origin https://github.com/用户名/kakobuy-blog.git
git add .
git commit -m "Initial blog setup"
git push -u origin main
```

### 5.3 Cloudflare Pages 连接

1. 登录 Cloudflare 控制台，进入 `kakobuy` 项目
2. Settings → Git → Connect to Git
3. 授权 GitHub，选择 `kakobuy-blog` 仓库
4. 配置：
   - Production branch: `main`
   - Build command: 根据选择的生成器填写（如 `npm run build`）
   - Build output directory: `dist` 或 `.vitepress/dist`
5. Save and Deploy

### 5.4 后续更新

每次更新文章后执行：

```bash
git add .
git commit -m "Update: 更新说明"
git push
```

Cloudflare 会自动重新部署。

## 6. 内容规范

### 6.1 文章格式

- 来源：本地已有的 .md 文件（需通过静态生成器转换为 HTML）
- 语言：根据关键词匹配（德语关键词用德语，英语用英语）
- 长度：每篇 1500-3000 字
- 关键词密度：1-3%，自然分布
- **输出形式**：每篇文章最终输出为独立的静态 HTML 文件，构建后无需任何后端处理即可直接访问

### 6.2 文章结构模板

```
H1: 主标题（含关键词）
---
H2: Introduction（引言）
H2: What Is...（核心概念解释）
H2: Why Buyers Use It（用户价值）
H2: Core Advantages（核心优势）
H2: Professional Analysis（专业分析）
H2: Comparison Section（平台对比）
H2: How to Use（使用指南）
H2: FAQ（常见问题）
H2: Conclusion（总结）
---
Related Resources（相关资源链接）
```

### 6.3 链接规则

- 页首：仅一个跳转到核心 Spreadsheet 的链接
- 正文：避免过多外部链接，保持内容专注
- 底部：Related Resources 部分使用裸 URL（非锚文字）展示额外资源

## 7. 验收标准

### 7.1 SEO 验收

- [ ] 所有页面可通过 Google Search Console 提交索引
- [ ] 每篇文章标题包含目标关键词
- [ ] Meta description 长度在 150-160 字符
- [ ] 结构化数据通过 Google Rich Results 测试
- [ ] 移动端友好测试通过
- [ ] Core Web Vitals 各项指标达标

### 7.2 功能验收

- [ ] 首页正常展示文章列表
- [ ] 所有文章链接可正常访问
- [ ] 响应式布局在手机/平板/桌面正常显示
- [ ] 404 页面正常跳转
- [ ] 加载速度 < 3 秒
- [ ] Cloudflare 自动部署正常触发

### 7.3 内容验收

- [ ] 所有 .md 文件已转换为 HTML
- [ ] 每篇文章 SEO meta 标签正确
- [ ] 文章结构完整，层级清晰
- [ ] 无死链、无图片缺失
- [ ] 关键词自然分布，无堆砌

## 8. 静态页面特别说明（重要）

### 8.1 什么是静态页面

静态页面是指在构建时（Build Time）就已经生成完整 HTML 内容的页面。与动态页面（如 WordPress、PHP 等）不同：

| 对比维度    | 静态页面             | 动态页面                     |
| ------- | ---------------- | ------------------------ |
| 内容生成时机  | 构建时生成            | 用户访问时实时生成                |
| 是否需要服务器 | 不需要              | 需要（PHP/Node.js/Python 等） |
| 是否需要数据库 | 不需要              | 需要（MySQL/MongoDB 等）      |
| 加载速度    | 极快（直接返回 HTML 文件） | 较慢（需查询数据库+渲染）            |
| SEO 友好度 | 极佳（爬虫直接读取内容）     | 一般（需 JS 渲染）              |
| 安全性     | 极高（无后端接口）        | 需防范 SQL 注入、XSS 等         |
| 维护成本    | 极低（无需维护服务器）      | 较高（需维护数据库和服务器）           |

### 8.2 本项目中的静态页面

- **首页**：静态 HTML 页面，列出所有文章标题和链接
- **文章页**：每个关键词文章都是独立的静态 HTML 文件
- **404 页面**：静态 HTML 页面，引导用户返回首页
- **sitemap.xml**：静态 XML 文件，列出所有页面 URL
- **robots.txt**：静态文本文件，引导爬虫抓取规则
- **CSS/JS**：静态样式和脚本文件，存储在 CDN 上直接分发

### 8.3 静态页面的工作流程

```
本地 .md 文件 
  → 静态生成器构建（VitePress/Hugo/Astro）
    → 生成静态 HTML/CSS/JS 文件
      → 推送到 GitHub
        → Cloudflare Pages 自动检测并部署
          → 文件分发到全球 CDN 节点
            → 用户访问 → 直接从最近的 CDN 节点获取静态文件
```

整个流程中，**没有任何服务器端处理**，所有文件在构建时已经生成完毕，用户访问时直接获取文件。

### 8.4 用户需要知道的

- **你不需要**：购买服务器、配置数据库、安装 PHP/Node.js、设置反向代理
- **你只需要**：写好 .md 文件 → 推送 GitHub → Cloudflare 自动部署 → 网站上线
- **更新文章**：修改 .md 文件 → 推送 GitHub → 几秒后自动重新部署
- **扩展性**：增加新文章只需要添加新的 .md 文件，网站会自动生成对应的静态页面

## 9. 注意事项

1. **GitHub 仓库必须是 Public**，否则 Google 无法通过 sitemap 发现页面
2. **不要使用 noindex 标签**，除非是管理页面
3. **关键词不要堆砌**，保持自然语言风格
4. **每次部署前检查 build 是否成功**，失败的部署会导致旧版本回滚
5. **定期查看 Cloudflare Pages 的 Build Log**，确保无错误
6. **建议注册 Google Search Console**，提交 sitemap 并监控索引状态
7. **静态页面特性**：所有页面内容在构建时已固定，修改内容后必须重新 push 才能生效

## 10. 参考资源

- Cloudflare Pages 文档：<https://developers.cloudflare.com/pages/>
- Google SEO 指南：<https://developers.google.com/search/docs/fundamentals/seo-starter-guide>
- VitePress 文档：<https://vitepress.dev/>
- Hugo 文档：<https://gohugo.io/>
- Structured Data Testing Tool：<https://search.google.com/test/rich-results>

***

**用户背景说明**：

- 用户为技术小白，无编程经验
- 需要在 AI 的逐步指导下完成部署
- 要求操作简单，避免复杂配置
- 最终目标：让文章通过 Google 搜索可被海外用户找到

